# Copy this file to .env and fill in your values.
# Lines starting with # are comments and are ignored.

# ------------------------------------------------------------------
# File to watch (defaults to ./tasks.txt if not set)
# ------------------------------------------------------------------
# TASKS_FILE=./tasks.txt

# ------------------------------------------------------------------
# Ollama endpoint (defaults to http://localhost:11434 if not set)
# ------------------------------------------------------------------
# OLLAMA_BASE_URL=http://localhost:11434

# ------------------------------------------------------------------
# SMTP / email settings
# Leave SMTP_HOST empty to skip email and print the answer instead.
# ------------------------------------------------------------------
SMTP_HOST=mail.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASSWORD=your_smtp_password

# EMAIL_SENDER defaults to SMTP_USER if not set
# EMAIL_SENDER=you@example.com

EMAIL_RECIPIENT=recipient@example.com
